<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/Lime</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MyCustomTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="android:actionBarItemBackground">@drawable/toolbar_background</item>
    </style>

    <style name="ItemTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabBackground">@color/Lime</item>
        <item name="tabIndicatorColor">@color/Olive</item>
    </style>

    <style name="ToolBarStyle" parent="ToolBarStyle">
        <item name="android:activatedBackgroundIndicator">@color/colorAccent</item>
    </style>



</resources>
